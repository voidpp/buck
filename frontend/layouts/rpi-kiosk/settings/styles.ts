import { SxProps } from "@mui/material";

export const commonStyles = {
    slider: {
        display: "flex",
        alignItems: "center",
        minWidth: 300,
    },
} satisfies Record<string, SxProps>;
