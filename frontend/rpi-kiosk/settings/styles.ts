import {makeStyles} from "@material-ui/core/styles";

export const useCommonStyles = makeStyles({
    slider: {
        display: "flex",
        alignItems: "center",
        minWidth: 300,
    },
});
